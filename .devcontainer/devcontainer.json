{
  "name": "Claude Code Persistent",
  "dockerComposeFile": "docker-compose.yml",
  "service": "claude-code-persistent",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "eamodio.gitlens"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
          "source.fixAll.eslint": "explicit"
        },
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "bash",
            "icon": "terminal-bash"
          },
          "zsh": {
            "path": "zsh"
          }
        }
      }
    }
  },
  "remoteUser": "node",
  "workspaceFolder": "/projects",
  "postCreateCommand": [
    // "bash", "-c",
    // "/projects/claude-code/.devcontainer/migrate-data.sh && mkdir -p /persistent/{.config,.cache/pip,.vscode-server,.oh-my-zsh,.claude} && ln -sf /persistent/.config /home/node/.config && ln -sf /persistent/.cache/pip /home/node/.cache/pip && ln -sf /persistent/.vscode-server /home/node/.vscode-server && ln -sf /persistent/.oh-my-zsh /home/node/.oh-my-zsh && ln -sf /persistent/.claude /home/node/.claude && ln -sf /persistent/.zsh_history /home/node/.zsh_history && sudo /usr/local/bin/init-firewall.sh"
  ],
  "containerEnv": {
    "PLAYWRIGHT_BROWSERS_PATH": "/usr/local/share/playwright-browsers"
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "configureZshAsDefaultShell": true,
      "installOhMyZsh": true,
      "username": "node",
      "upgradePackages": true
    }
  }
}
